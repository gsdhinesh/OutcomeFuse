{
  "evidence_refs": [
    {
      "id": "orderflow/validation.py:28"
    }
  ],
  "fix_summary": "The stored entry is read in the wrong order, so the age is never compared against the TTL; add a guard before the value is returned.",
  "root_cause_file": "orderflow/validation.py",
  "root_cause_line": 28,
  "severity": "major",
  "suggested_test": "a regression test that advances the clock past the TTL"
}