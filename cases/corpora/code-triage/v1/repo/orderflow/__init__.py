"""Order flow service — synthetic corpus for the code-triage workload.

Contains deliberately seeded defects. Nothing in this package marks where they
are: the case answer keys locate each defect by searching for an ordinary line
of code, so the corpus gives no signal an agent could exploit.
"""
